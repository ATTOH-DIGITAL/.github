# What the Cloud Fleet Cannot Finish — Owner Actions + Local Claude Paste

Generated by the overseer session `claude/lane-live-data-updates-jkb448` · snapshot 2026-08-17T01:46Z · regenerated whenever blockers change. Counts below are timestamped reference figures — every task pulls the live list at run time.

## Part 1 — Owner actions (no Claude needed, ~5 minutes total)

These two unblock the **remote** fleet so it can finish autonomously — do these first and most of Part 3 disappears:

1. **GitHub Actions billing** — Actions across ATTOH-DIGITAL repos has been dead since ~1 Aug (`startup_failure` on every run: billing). Fix in GitHub → Organization settings → Billing. Unblocks: attoh.casa worker deploy (5 restored products), costashop artwork deploy, and CI estate-wide.
2. **Authorize connectors** in claude.ai → Settings → Connectors: **Stripe** and **Cloudflare Developer Platform**. Both currently show "needs authentication" to cloud sessions. Unblocks: payment-link wiring (P4) and worker deploys (P2) directly from the cloud lanes.

## Part 2 — Or run it locally: full paste for Claude Code

If you'd rather finish these locally (or the connectors stay unauthorized), paste everything in the block below into Claude Code on a machine that has: `wrangler` logged into the Cloudflare account serving `craig-45c.workers.dev`, a **restricted live** Stripe API key, Railway CLI/dashboard access, and Supabase access to project `ofcjggvruhwhaqthdwxa` (MCP or service-role key in the local env).

```text
You are working the Pharoah estate register (Supabase project ofcjggvruhwhaqthdwxa). Owner: Phil.
Doctrine (binding): PHAROAH FIRST — live data only — no guessing, no assuming. Session memory is
stale: SELECT fresh immediately before every read that feeds an action and before every write;
probe URLs live at the moment of verification; every done/verified you record carries same-step
evidence (HTTP code + timestamp, commit SHA, run/dashboard URL). Never touch GhostWire rows or
repos (cluster ghostwire-suite/ghostwire, ghostmaps). Never spend money beyond creating the Stripe
objects listed without asking Phil. The cloud fleet reads lane_handoffs — report there, not just in chat.

TASK A — MONEY-WIRE (P4; ~105 products without payment links, 54 without Stripe prices at 01:45Z 17 Aug):
1. Pull the live list:
   SELECT product_key, display_name, price_gbp, price_annual_gbp, billing_interval, stripe_mode,
          stripe_product_id, stripe_price_id, payment_link_url
   FROM products
   WHERE cluster = 'pharoah' AND coalesce(payment_link_active,false) = false
   ORDER BY coalesce(readiness_pct,0) DESC, product_key;
2. Work highest-readiness first. Skip rows with price_gbp NULL — post those keys to lane_handoffs as
   'needs pricing decision' instead of inventing prices.
3. For each remaining product: create Stripe product + price (GBP, interval from billing_interval;
   also annual price when price_annual_gbp is set) + payment link, in the mode stripe_mode says
   (default live). Reuse existing stripe_product_id/stripe_price_id if set — verify in Stripe first.
4. Write back immediately per product (no batching at the end):
   UPDATE products SET stripe_product_id=$1, stripe_price_id=$2, payment_link_url=$3,
     payment_link_active=true, payment_link_synced_at=now(), updated_at=now()
   WHERE product_key=$4;
5. Verify each payment link with a live HTTP GET (expect 200) before setting payment_link_active.

TASK B — DEPLOY-WIRE (P2; ~36 pharoah products with no deploy_url at 01:45Z):
1. Pull the live list:
   SELECT product_key, display_name, source_repo, source_path, cf_pages_project, deploy_platform
   FROM products WHERE cluster='pharoah' AND deploy_url IS NULL ORDER BY product_key;
2. For each: clone/fetch source_repo fresh; look for wrangler.toml / Pages config at source_path.
   Already deployed but unregistered → find the real URL (wrangler deployments list / CF dashboard),
   probe it live, and register it. Deployable but not deployed → wrangler deploy (workers.dev is
   acceptable as first wiring; custom domains are a later pass). No repo or no buildable source →
   post the key to lane_handoffs as 'needs-build', do NOT guess a URL.
3. Write back per product:
   UPDATE products SET deploy_url=$1, deploy_probe_code=$2, deploy_probed_at=now(),
     deploy_probe_class='ok', updated_at=now() WHERE product_key=$3;
   INSERT INTO product_live_probe (product_key, http_code, bytes, live, probed_at)
   VALUES ($3, $2, $bytes, true, now());
   Only after a live 2xx probe you ran in that same step.

TASK C — 404 FIXES (P3):
1. gym-health-glasses: https://gateway-production-951c.up.railway.app/glasses/gym returns 404.
   Open the Railway project (gateway-production-951c), check the service/route, redeploy or fix the
   route, probe live, update the register row with evidence.
2. If GitHub Actions billing is still broken: deploy the attoh.casa worker and costashop directly
   with wrangler from their repos (repo-side code is already restored/committed — fetch fresh and
   check lane_handoffs rows from pharoah-lane-attoh-casa-restore for exact paths), then probe the
   5 attoh.casa product paths + costashop live and update their register rows.

TASK D — REPORT BACK (always, so the cloud fleet doesn't redo or contradict your work):
INSERT INTO lane_handoffs (from_lane, to_lane, topic, body, body_evidence, severity, status, related_session)
VALUES ('mac_code', 'pharoah', 'LOCAL RUN COMPLETE: money-wire/deploy-wire/404 results',
        '<what you completed, what remains>', <jsonb evidence: counts, probe codes, timestamps>,
        'high', 'open', 'local-claude-code');
Then re-run the Task A and Task B SELECTs and include the fresh remaining-counts in the row.
```

## Part 3 — Current owner-action blocker ledger (reference snapshot, 01:46Z 17 Aug)

| # | Blocker | Blocks | Cleared by |
|---|---|---|---|
| 1 | GitHub Actions billing (org-wide, since ~1 Aug) | attoh.casa ×5 deploy, costashop re-verify, all CI | Owner: fix billing (Part 1.1) — or local wrangler deploy (Task B/C) |
| 2 | Stripe connector unauthorized in claude.ai | P4 money-wire: 105 payment links, 54 prices | Owner: authorize (Part 1.2) — or local run (Task A) |
| 3 | Cloudflare connector unauthorized in claude.ai | P2 deploys for the ~36 unwired products | Owner: authorize (Part 1.2) — or local run (Task B) |
| 4 | Railway access (no cloud credentials) | gym-health-glasses 404 | Local run (Task C.1) |
| 5 | Pricing decisions for rows with price_gbp NULL | subset of P4 | Owner: set prices (list lands on the bus as 'needs pricing decision') |

Everything else in the Pharoah queue (P1 verification, URL discovery for already-deployed products, register hygiene, listing prep) is executable by the cloud lanes and is being driven by the hourly autopilot — no owner action needed.
