# What the Cloud Fleet Cannot Finish — Owner Actions + Local Claude Paste

Generated by the overseer session `claude/lane-live-data-updates-jkb448` · snapshot 2026-08-17T13:35Z (supersedes 02:52Z — attoh.casa ×5, costashop and the gym-health-glasses 404 are CLEARED; Stripe work PARKED by owner order) · regenerated whenever blockers change. Counts below are timestamped reference figures — every task pulls the live list at run time.

## Part 1 — Owner actions (one consolidated list — nothing else needs you)

**1. CRITICAL — kill the leaked key. Needs your sign-in; the local run proved it cannot be done from any terminal.**
The leaked `service_role` JWT (project `ofcjggvruhwhaqthdwxa`, expires 2036, sitting in recruitment-os git history) is **still enabled**. Both the Supabase and Cloudflare dashboards redirect to sign-in for the local session, and the stored access token is an 11-char placeholder.
**Recommended action (better than rotating): DISABLE the legacy `anon` + `service_role` pair** in Supabase → Project Settings → API Keys → Legacy keys. The estate already runs on `sb_secret_`/`sb_publishable_`, so disabling kills the leaked key without minting another leakable JWT. Prep is done — recruitment-os `9694bfb` moved the A1 portal onto `sb_publishable_`, so this no longer breaks it. Two other local surfaces still embed legacy anon keys and need a follow-up pass after you disable: `glasses-sentinel/wrangler.jsonc` and `pharoah-website/api/_lib/supabase.js`.

**2. Publish listingforge — one command from revenue.** #6 is merged (tests 8/8) but never deployed, so `listingforge.pharoahtechnology.com` still serves the OLD page with the disabled buy button. `wrangler deploy` from main, then smoke-test: page shows "Go Pro — £39/month" and the button opens Stripe Checkout. Same for **proposalforge** (#9 merged, not deployed).

**3. Connect Workers Builds (needs dashboard sign-in).** The Cloudflare GitHub App is already installed on ATTOH-DIGITAL (id 132149322), but `builds/repos` returns auth error 10000 on all three estate tokens and none can mint a scoped one. Connecting it once means merges auto-deploy and items like #2 stop needing a human.

**4. sol-ai-app PR #8** — Stripe Checkout fulfilment, lane-reviewed and sound; your merge click.

**5. Decisions:** vyral-studio #4 (DEPLOY.md Supabase choice + `VITE_SUPABASE_*` in the Pages build) · kid-safe-companion AADC-vs-revenue call on its buy-path spec · prices for `price_gbp NULL` rows. **Stripe stays PARKED** until you say unpark.

**6. propvault-deals — needs money or a code change, your call.** Production is ~37 days stale because `vercel --prod` is refused: 5 crons in `vercel.json` vs the Hobby limit of 2 daily. Either authorise a Vercel Pro upgrade or approve cutting the schedule to 2 daily crons. Also note the lane deliberately did **not** set `RESEND_API_KEY`: the waitlist has 0 recipients, the `propvault.es` From-domain is unregistered, and `/unsubscribe` doesn't exist — setting it would have created a broken, non-compliant sender.

**7. GitHub Actions billing** — CI-only. GitHub → org settings → Billing, whenever.

**Live now, proven by probe (13:05Z local run):** `sol-ai.pharoahtechnology.com/api/health` → HTTP 200 `{ok:true, gateway:ok, supabase:ok}` (worker v2f3d6d9c) · `access.pharoahtechnology.com` → HTTP 200 serving 148 register-listed doors (worker vf84be94b). Both merged changes were **broken as merged** and fixed before deploy: sol-ai's health probe called Supabase with no apikey (401 → pinned at 503 forever, fixed `282706d`); pharoah-access fetched from the deleted pharoah-cockpit host (hard 404 → "register feed unreachable" always, fixed `0d64a54`). Merges alone would have shipped two dead products.

**Estate redefined (owner decision 13:31Z):** 14 empty rows (no repo, no code, no URL) de-scoped, so the active estate is **85 products, 66% avg readiness, 31 at 100%** — and 100% is now actually reachable. Fleet re-ordered onto the biggest unblocked pile: **40 products already live and only missing a verification pass** (no owner input needed), then **15 built-but-undeployed** waiting on Cloudflare.

**Cleared:** Attohdigital #891 + pharoah-core #80 (owner-merged 12:51Z) · listingforge #6, pharoah-access #1, proposalforge #9, propvault-espana #3 merged · watchdog fault fixed · autopilot deduped · attoh.casa ×5 + costashop live. **Register defect fixed:** `pip` source_repo found and set (pharoah-tech, `products/pip/`, 210 files, branch `claude/pharoah-intelligence-platform-3vuddx@38c7908`) — readiness honestly left at 0: not merged, not deployed.

## Part 2 — Or run it locally: full paste for Claude Code

If you'd rather finish these locally (or the connectors stay unauthorized), paste everything in the block below into Claude Code on a machine that has: `wrangler` logged into the Cloudflare account serving `craig-45c.workers.dev`, a **restricted live** Stripe API key, Railway CLI/dashboard access, and Supabase access to project `ofcjggvruhwhaqthdwxa` (MCP or service-role key in the local env).

```text
You are working the Pharoah estate register (Supabase project ofcjggvruhwhaqthdwxa). Owner: Phil,
sitting with you now. Doctrine (binding): PHAROAH FIRST — live data only — no guessing, no assuming.
Session memory is stale: SELECT fresh immediately before every read that feeds an action and before
every write; probe URLs live at the moment of verification; every done/verified you record carries
same-step evidence (HTTP code + timestamp, commit SHA, run/dashboard URL). Never touch GhostWire
rows or repos (clusters ghostwire/ghostwire-suite, ghostmaps). Never spend money without asking
Phil. The cloud fleet reads lane_handoffs — report there (Task D), not just in chat. Counts in
parentheses are reference snapshots from 12:01Z 17 Aug — always re-pull live.

TASK 0 — ROTATE THE EXPOSED KEY (do first, with Phil):
The estate service_role key for ofcjggvruhwhaqthdwxa was shipped client-side (recruitsnap A1
portal; code removal merged 02:25Z, key still valid). If the supabase CLI here is authenticated,
rotate the service_role key for project ofcjggvruhwhaqthdwxa via CLI/management API; otherwise
walk Phil through it: Dashboard -> Project Settings -> API -> rotate service_role. Then: grep local
env files / edge-function secrets for the old key and update legitimate server-side usages; verify
the old key now fails and the new one works with a harmless SELECT; post the evidence to the bus
and mark the 02:07Z critical lane_handoffs row resolved (status='resolved', resolved_at=now(),
resolution_notes with evidence).

TASK M — DRAIN THE MERGE QUEUE (the throughput bottleneck; cloud lanes are classifier-blocked
from self-merging):
1. Verify then merge each of: ATTOH-DIGITAL/Attohdigital#891 (pharoah pages fixes),
   ATTOH-DIGITAL/pharoah-core#80 (alert pusher + billing fixes), ATTOH-DIGITAL/listingforge#6
   (Pro buy button -> live checkout, tests 8/8). For each: confirm open + mergeable + no
   changes-requested reviews, then merge (gh pr merge or the UI with Phil).
2. Pull the coordinator's current branch queue live:
   SELECT topic, body, created_at FROM lane_handoffs
   WHERE topic ILIKE '%LEDGER%' OR topic ILIKE '%merge queue%'
   ORDER BY created_at DESC LIMIT 5;
   Merge each listed branch the same way (open a PR first where one doesn't exist).
3. After merging: Cloudflare Git integration auto-deploys from main — probe each affected
   deploy_url live (expect 200) and update the register rows + product_live_probe with evidence.

TASK R — UNBLOCK THE 7 ATTESTATIONS (cloud lane lacks repo access; you have local git):
SELECT body, body_evidence FROM lane_handoffs
WHERE topic LIKE 'ATTESTATION BATCH 1%' ORDER BY created_at DESC LIMIT 1;
For each of the 7 blocked products: open its source_repo locally (clone/fetch fresh), verify from
source what is actually built, append dated evidence to products.notes, upgrade product_parts
statuses only with evidence, and fix the named register defect from that row.

TASK B — DEPLOY-WIRE (36 pharoah products with no deploy_url at 12:01Z):
1. SELECT product_key, display_name, source_repo, source_path, cf_pages_project, deploy_platform
   FROM products WHERE cluster='pharoah' AND deploy_url IS NULL ORDER BY product_key;
2. Each: fetch source_repo fresh; wrangler.toml / Pages config at source_path. Already deployed
   but unregistered -> find the real URL (wrangler deployments list / CF dashboard), probe live,
   register it. Deployable but not deployed -> wrangler deploy (workers.dev fine as first wiring).
   No repo / no buildable source -> post the key to lane_handoffs as 'needs-build'; never guess.
3. Write back per product only after a live 2xx probe in the same step:
   UPDATE products SET deploy_url=$1, deploy_probe_code=$2, deploy_probed_at=now(),
     deploy_probe_class='ok', updated_at=now() WHERE product_key=$3;
   INSERT INTO product_live_probe (product_key, http_code, bytes, live, probed_at)
   VALUES ($3, $2, $bytes, true, now());

TASK V — VYRAL-STUDIO #4 (needs Phil's decision, then done in minutes):
Ask Phil the DEPLOY.md Supabase question (options are in the PR), set VITE_SUPABASE_* in the
Pages build env accordingly, merge, probe, update the register row.

TASK A — MONEY-WIRE: PARKED BY OWNER ORDER. Skip entirely UNLESS Phil says "unpark Stripe" in
this session. If he does: SELECT product_key, display_name, price_gbp, price_annual_gbp,
billing_interval, stripe_mode, stripe_product_id, stripe_price_id FROM products WHERE
cluster='pharoah' AND coalesce(payment_link_active,false)=false ORDER BY
coalesce(readiness_pct,0) DESC; skip price_gbp NULL rows (post them as 'needs pricing decision');
for the rest create Stripe product + price (GBP, interval per row; annual too when set) + payment
link in the mode stripe_mode says, reusing existing IDs after verifying them in Stripe; write back
per product immediately (stripe_product_id, stripe_price_id, payment_link_url,
payment_link_active=true, payment_link_synced_at=now()) — active only after a live 200 GET on the
link.

TASK D — REPORT BACK (always, last):
INSERT INTO lane_handoffs (from_lane, to_lane, topic, body, body_evidence, severity, status, related_session)
VALUES ('mac_code', 'pharoah', 'LOCAL RUN COMPLETE: rotation/merges/attestations/deploy-wire results',
        '<done + remaining>', <jsonb: counts, SHAs, probe codes, timestamps>, 'high', 'open',
        'local-claude-code');
Then re-run the Task B SELECT and the pharoah gap recount and include fresh remaining-counts in
the row so the cloud fleet and the overseer see the new baseline.
```

## Part 3 — Current owner-action blocker ledger (reference snapshot, 02:52Z 17 Aug)

| # | Blocker | Blocks | Cleared by |
|---|---|---|---|
| 1 | **service_role key still valid after client-side exposure (CRITICAL)** | register security | Owner: rotate on `ofcjggvruhwhaqthdwxa` (Part 1.1) |
| 2 | listingforge PR #6 bot-merge classifier-blocked | Pro checkout going live | Owner: one-click merge (Part 1.2) |
| 3 | vyral-studio PR #4 Supabase decision + `VITE_SUPABASE_*` env | vyral-studio deploy | Owner decision (Part 1.3) |
| 4 | Billing-rails decision + `price_gbp NULL` pricing; Stripe **PARKED by owner order** | P4 money-wire (102 links / 54 prices) | Owner: decide + unpark (Part 1.4) — then Task A |
| 5 | GitHub Actions billing (org-wide, since ~1 Aug) | CI only — deploys have workarounds | Owner: fix billing (Part 1.5) |

Cleared since 01:46Z: attoh.casa ×5 + costashop (deployed locally, prober-confirmed 200), gym-health-glasses (false URL — register corrected). Everything else in the Pharoah queue (P1 verification, P2 URL-wire for the 36 unwired, P3 assembly of 0%-built products, register hygiene, listing prep) is running on the cloud lanes under the hourly autopilot — no owner action needed.
